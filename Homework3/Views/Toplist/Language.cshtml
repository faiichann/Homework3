@model IEnumerable<Homework3.Models.Languages>

@{
    ViewData["Title"] = "Language";
}
<div class="container">
    <div class="row justify-content-md-center">
        <div><h1 class="text-center display-4">@ViewBag.title1</h1><br /><br></div>
        <table class="table table-hover table-dark text-center" style="width: 1000px; box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19) ">
            <thead>
                <tr class="bg-info">
                    <th scope="col">Rank</th>
                    <th scope="col">Programming Language</th>
                    <th scope="col">Rating</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in ViewData["langlist"] as IEnumerable<Homework3.Models.Languages>)
                {
                    <tr>
                        <th scope="row">@item.LangId</th>
                        <td>@item.LangName</td>
                        <td>@item.LangRate</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="row justify-content-md-center">
        <a class="btn btn-primary btn-lg" asp-controller="Toplist" asp-action="Index" role="button">Home</a>
    </div>
</div>